{
  "name": "fahad-atc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Quest 3 WebXR sandbox for Kuwait Intl (OKBK) ATC — Phase 1",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "fetch:snapshot": "node scripts/fetch-snapshot.mjs",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "3d-tiles-renderer": "^0.4.24",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.2.0",
    "vite-plugin-mkcert": "^1.17.5"
  }
}
